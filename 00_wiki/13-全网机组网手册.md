# 13 · 全网机组网手册（Tailscale，6 台）

> **状态 2026-09-05**：5/6 已入网 ✅（本机/copizza/copizza2/cipizza3/copizza4，全部 launchd 持久化 + SSH 免密互通）。
> 仅剩 Windows U (192.168.31.33)：SSH 被防护锁定（administrator 触发防爆破），RDP 可用。
> **人工 1 分钟**：RDP 进去 → 管理员 PowerShell 粘贴 → `irm https://github.com/Shiyao-Huang/ChinaMicroDuck/releases/download/fleet-v1/onboard_windows.ps1 -OutFile o.ps1; Set-ExecutionPolicy Bypass -Scope Process -Force; ./o.ps1`

> Tailnet 账号：hsy863551305@gmail.com（密码 HSYjiayou1125）
> 本机已完成 ✅（100.71.219.0）

## 待入网 5 台的安装命令

### copizza / copizza2 / copizza3 / copizza4（Mac mini ×4）

每台在终端跑（服务器密码 copizzajiayou1125!）：

```bash
curl -fsSL https://tailscale.com/install.sh | sh
sudo tailscale up
# 弹出链接 → 复制到该机器浏览器打开 → 选 hsy863551305@gmail.com → Connect
```

### Windows（U）

PowerShell（管理员）：

```powershell
# 下载安装包
winget install Tailscale.Tailscale
# 或 https://tailscale.com/download/windows 下载 MSI
# 装完托盘点图标 → Log in → 浏览器同账号登录 → Connect
```

## 完成后验证

任意一台跑：
```bash
tailscale status
```
应看到 6 台设备（本机+4×Mac mini+Windows）。

之后所有机器互通：
```bash
ssh copizza@<目标机 tailscale IP 或机器名>
```

## MagicDNS 命名（可选，建议开）

登录 https://login.tailscale.com → DNS → Enable MagicDNS
之后直接 `ssh copizza@copizzademac-mini` 无需记 IP。
