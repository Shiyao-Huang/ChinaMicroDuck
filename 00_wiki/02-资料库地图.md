# 02 · 资料库地图

```
Microduck/
├── 00_wiki/                    ← 本 wiki（LLM 友好索引见 HOME.md）
│   └── zh/                     ← 官方文档中文翻译
├── 01_official/                ← 官方仓（git clone，含 .git 可追上游）
│   ├── microduck/              ← Rust 运行时（284 文件）
│   ├── microduck_rl/           ← RL 训练栈 + MJCF + 47 STL（224 文件）
│   ├── microduck-gst-plugins/
│   └── microduck-policies/     ← 9 ONNX + manifest（SHA256 见根 MANIFEST.sha256）
├── 02_cad/
│   ├── upstream_stl/           ← 47 STL + .part（25MB，从 microduck_rl 提取）
│   ├── replica_assembly/
│   │   ├── microduck-replica/          ← 装配图7张 + CAD装配体15件 + 四大逆向文档（fanhao375）
│   │   ├── microduck-hardware-replica/ ← FreeCAD 70件装配 + Excel BOM 39项（lingzolabs）
│   │   └── open-microduck/             ← 逆向文档（SaberOnGo，中英）
│   ├── openduckmini/
│   │   ├── Open_Duck_Mini/             ← 前代全开源：print/STL + 装配/打印/接线指南 + BEST_WALK ONNX
│   │   └── Open_Duck_Mini_Runtime/     ← Pi Zero 2W 运行时
│   └── diy_printable/
│       └── microduck-diy/              ← 手搓月度构建日志（ScrapMeta，中文）
├── 03_pcb/                     ← 打板资料（规格书）
├── 04_firmware/                ← 预留：运行时移植笔记
├── 05_sim/                     ← 仿真/训练环境（已实测）
│   ├── .venv/                  ← E1 环境（mujoco+onnxruntime+bam）
│   ├── microduck-lab-apple/    ← E3 Apple Silicon 训练仓（SB3 PPO）
│   ├── stl_check.py            ← E5 网格体检脚本
│   └── run_e1_sim.sh           ← E1 一键复现
├── 06_bom/                     ← 方案细化 BOM（见 00_wiki/07）
├── 07_build_log/               ← 构建日志（stl_mesh_report.json 等）
├── 08_refs/
│   ├── awesome-microduck/      ← 社区生态全清单
│   └── press-kit/              ← 官方新闻资料包
├── MANIFEST.sha256             ← 全库哈希清单
└── scripts_*.sh                ← 建库脚本（可重放）
```

## 校验

```bash
cd ~/Desktop/work/robot/Microduck
shasum -a 256 --check MANIFEST.sha256   # 全库完整性
```

## 上游同步

各仓保留 .git：`git -C 01_official/microduck pull --ff-only` 等逐仓更新后重跑
`zsh scripts_manifest.sh` 刷新 MANIFEST。
