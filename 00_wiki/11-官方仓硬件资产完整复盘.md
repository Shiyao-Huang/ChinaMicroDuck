# 官方 GitHub 仓硬件资产 · 完整复盘清单（2026-09-02 全量扫描）

> 方法：逐文件扫描 `pollen-robotics/microduck` 全仓（284 文件），
> 提取所有与硬件相关的可交付物。本清单回答："官方到底给了什么，我们 BOM/资料包还缺什么。"
> 越接近原始（官方原件），复刻越容易成功——以下按「官方已给 / 官方没给」分列。

## 一、官方已给的可直接复用资产（此前 BOM 未完整覆盖的）

| # | 资产 | 位置 | 对复刻的价值 |
|---|---|---|---|
| 1 | **9 个 ONNX 策略（官方仓内置副本）** | `policies/*.onnx` + README（含来源 provenance 表） | 与 HF Hub 同源；README 给出每个策略的原始训练 run 名与用途映射（roller/roulade/kick 等）——**不需要额外下载** |
| 2 | **NPU 检测模型（含 RKNN！）** | `duck-detect/models/duck_detect.rknn` + `.onnx`；`pet-detect/models/pet_detect.onnx` + `training/train.py` | **此前完全遗漏**：`duck_detect.rknn` 是编译好的 RK3566 NPU 模型，绕过自己转换 NPU 模型这一步；pet-detect 连训练脚本都给了 |
| 3 | **声音资产** | `sounds/scores/`：duck_strut.mid / outer_wilds.mid / wistful.duckscore | 合唱（chorale）功能的乐谱资产；配套 Rust 合成器源码 `sounds/src/`（synth/voices/personality） |
| 4 | **设备树 overlay 全套** | `deploy/audio/`：aic3104-i2c3.dts、i2c3-pihat.dts、aic3104-init.sh（amixer 全套混音器设置）、aic3x-dkms（自编译内核 codec 模块！）；`deploy/overlays/rk3568-npu-enable.dts` | HAT 音频路径的**完整 dts 源码**——若做音频功能，设备树不用自己写；DKMS 模块连 Makefile 都有 |
| 5 | **板级 bring-up 脚本** | `scripts/setup-board.sh`（overlay_prefix 修正 rk35xx→rk3568、uart2-m0、getty mask、NPU setup）、`migrate-network.sh`、`setup-gstreamer.sh`、`setup-npu.sh`、`setup-rkaiq.sh`、`rkaiq-modinfo-shim.c` | **Board bring-up 完全脚本化**——三个坑（getty/i2c3/NPU）官方脚本全包，不需要手工操作 |
| 6 | **交叉编译工具链脚本** | `scripts/board-test.sh`（cargo-zigbuild + Docker 原生 arm64 验证，glibc 地板 2.36）+ `cross-sysroot.sh` + `dev-build.Dockerfile` | **E4 的正确解法就在这里**：不用 Mac 裸交叉，用他们准备好的容器/sysroot 方案 |
| 7 | **装机脚本** | `scripts/install.sh`（958 行，含 servo 端口检查）、`provision-board.sh`、`robot-boot-check`、`robot-rescue` | 刷机后一键装机；boot 失败自动进 rescue 的完整逻辑 |
| 8 | **robotd.toml 全量参数注释版** | `deploy/robotd.toml` | 每个参数的物理含义+调参警告全写在注释里（kP 120 事故、50Hz 来源、mediad 码率/CCC 每档的含义）——**调参圣经** |
| 9 | **控制常量与契约** | `duck-control/src/model.rs`：JOINT_IDS=[20-24,30-34,10-14]、IMU_DXL_ID=200、MOUTH_INDEX=9、HOME pose；`bus.rs`：sync_read 寄存器/顺序（IMU first） | 软硬件接口的**代码级规格**——imu_to_dxl 板的应答顺序（IMU 第一个回）此前未记录，本轮补上 |
| 10 | **robotctl 3D 资产管线** | `scripts/bake-duck-mesh.py`：MJCF+STL → duck.bin（含 HIDDEN parts 清单：batteries/PCBs/brackets 被外壳包住不参与渲染） | 间接确认机体内部件清单：电池、PCB、支架 |
| 11 | **Updater 签名公钥** | `deploy/trusted_keys/release-{1,2,3}.pub` + updater.toml | 自建 OTA 时可复用整套签名/回滚架构 |
| 12 | **NPU 检测上游** | `duck-detect` 训练仓链接：github.com/pollen-robotics/duck_detector | 想改检测模型有上游 |

## 二、官方没给的（缺口确认，替代来源）

| # | 缺口 | 官方状态 | 我们的替代方案 |
|---|---|---|---|
| 1 | 机械 CAD（可编辑） | ❌ 无（仅 STL 网格 + bake 脚本里的隐藏件清单） | fanhao375 装配体 + lingzolabs FreeCAD（已在库） |
| 2 | 原理图/PCB（两块自制板） | ❌ 无（仅 dts/脚本证明功能） | 自画（规格书就绪 `03_pcb/`） |
| 3 | imu_to_dxl 固件 | ❌ 无（"板子自己的固件，不在这个仓库"） | 自写 G2/G3（资料包已列依赖链） |
| 4 | BOM 采购表 | ❌ 无 | 社区逆向 + 本资料包店铺表 |
| 5 | 装配文档/公差 | ❌ 无 | fanhao375 构建日志 + 紧固件反推 |
| 6 | 走线图 | ❌ 无 | 自行设计（颈部动态弯折区） |
| 7 | NFC 硬件与软件 | ❌ **官方仓零引用**（全仓 grep=0）——NFC 是宣传规格（2 天线），SDK 未发布 | 硬件自加（PN532 类模块）；或一期不做 |
| 8 | 滑轮(rollers)/ball/镭射笔/NFC 拍立得 | ❌ 软件仓不含（Accessory pack 实物配件） | 淘宝自购；滑轮对应 roller 策略（软件已含） |
| 9 | 音频功率放大 | ❌ codec 直出 LOP，功放未提 | MAX98357A 模块或小功放 |
| 10 | 电机驱动板（HAT） | ❌ 无原理图（dts/注释证明 TLV320+BMI088+ToF 口+供电过板） | 免音频即免 HAT；半双工方向电路并入 imu 板 |

## 三、修正此前认知的两个点

1. **智能佳 SKU 复核**：`valueItem` 外层的 `isDisabled` 样式是"未选中"态而非缺货——
   **¥95 的 M288 实际可选、有货**（立即购买/加入购物车按钮在，页面无缺货提示）。A 方案舵机成本回到 ¥1,425 档。
2. **NFC 是宣传项不是开源项**：官方仓 0 处 NFC 代码/配置。宣传"2× NFC 天线"对应硬件存在，
   但软件栈完全没有——复刻时 NFC 属于"自选research项"，不进一期 BOM。

## 四、行动项（更新到执行序）

- [x] E4 交叉编译改用官方 `scripts/board-test.sh` 方案（cargo-zigbuild + Docker），放弃裸交叉
- [x] A 方案 NPU 检测直接用官方 `duck_detect.rknn`（省一步模型转换）
- [x] 板级 bring-up 直接跑官方 `setup-board.sh`（三个坑全脚本化）
- [ ] 一期不做：NFC、镭射笔、拍立得、音频功放（全部二阶段）
- [ ] 立即采购：智能佳 XL330-M288 ¥95 ×15（确认有货）
